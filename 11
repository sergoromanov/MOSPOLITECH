#include <iostream>
using namespace std;
void task1() {
	cout << "Task1: Даны две переменные целого типа: A и B. Если их значения не равны, то присвоить каждой переменной большее из этих значений, а если равны, то присвоить переменным нулевые значения. Вывести новые значения переменных A и B." << "\n";
	int A, B;
	cout << "A:";
	cin >> A;
	cout << "B:";
	cin >> B;
	if (A != B) {
		A = A + B;
		B = A;
		cout << "A:" << A << "\n";
		cout << "B:" << B;
	}
	else {
		A = B = 0;
		cout << "A:" << A << "\n";
		cout << "B:" << B;
	}
	cout << "\n";
}
void task2() {
	cout << "Task2: Даны три числа. Найти сумму двух наибольших из них" << "\n";
	float a, b, c;
	cout << "A:";
	cin >> a;
	cout << "B:";
	cin >> b;
	cout << "C:";
	cin >> c;
	if (b <= c && b <= a) {
		cout << "Наибольшее из двух: " << a + c;
	}
	if (a <= c && a <= b) {
		cout << "Наибольшее из двух: " << b + c;
	}
	else {
		cout << "Наибольшее из двух: " << a + b;
	}
	cout << "\n";
}
void task3() {
	cout << "Task3: На плоскости расположены три точки: A, B, C. Определить, какая из двух последних точек (B или C) расположена ближе к A, и вывести эту точку и ее расстояние от точки A." << "\n";
	int a, b, c;
	cout << "A:";
	cin >> a;
	cout << "B:";
	cin >> b;
	cout << "C:";
	cin >> c;
	if (abs(a - b) < abs(a - c)) {
		cout << "Точка:" << b << endl;
		if ((a - b) > 0)
			cout << "Расстояние:" << a - b;
		else if ((a - b) < 0)
			cout << "Расстояние:" << abs(a - b);
	}
	else {
		cout << "Точка:" << c << endl;
		if ((a - c) > 0)
			cout << "Расстояние:" << a - c;
		else if ((a - c) < 0)
			cout << "Расстояние:" << abs(a - c);
	}
	cout << "\n";
}
void task4() {
	cout << "Task4: Даны координаты точки, не лежащей на координатных осях OX и OY. Определить номер координатной четверти, в которой находится данная точка" << "\n";
	int x, y;
	cout << "X:";
	cin >> x;
	cout << "Y:";
	cin >> y;

	if (x > 0 && y > 0) {
		cout << "1 четверть";
	}
	else if (x < 0 && y>0) {
		cout << "2 четверть";
	}
	else if (x < 0 && y < 0) {
		cout << "3 четверть";
	}
	else {
		cout << "4 четверть";
	}
	cout << "\n";
}
void task5() {
	cout << "Task5: Дано целое число. Вывести его строку-описание вида «отрицательное четное число», «нулевое число», «положительное нечетное число» и т. д." << "\n";
	int a;
	cout << "Число:";
	cin >> a;
	if (a == 0) {
		cout << "нулевое число ";
	}
	else {
		if (a > 0) {
			cout << "положительное ";
		}
		else {
			cout << "отрицательное ";
		}
		if (a % 2 == 0) {
			cout << "четное ";
		}
		else {
			cout << "нечетное ";
		}
	}
	cout << "\n";
}
void task6() {
	cout << "Task6: Дано целое число, лежащее в диапазоне 1–999. Вывести его строкуописание вида «четное двузначное число», «нечетное трехзначное число» и т. д." << "\n";
	int a;
	cout << "Число, лежащее в диапазоне 1–999:";
	cin >> a;
	if (a % 2 == 0) {
		cout << "четное ";
	}
	else {
		cout << "нечетное";
	}
	if (a / 100 > 0 && a / 100 < 10) {
		cout << "трехзначное ";
	}
	else if (a / 10 > 0 && a / 10 < 10) {
		cout << "двухзначное ";
	}
	else if (a / 10 == 0) {
		cout << "однозначное ";
	}
	cout << "число";
}
int main() {
	setlocale(LC_ALL, "rus");
	task1();
	cout << "\n";
	task2();
	cout << "\n";
	task3();
	cout << "\n";
	task4();
	cout << "\n";
	task5();
	cout << "\n";
	task6();
}


